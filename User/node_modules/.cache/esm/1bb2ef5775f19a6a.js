let JWT_SECRET,jwt;_75f‍.x([["default",()=>_75f‍.o]]);_75f‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_75f‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

class JWTService {
    static sign(payload, expiry = '60s', secret = JWT_SECRET) {
        return jwt.sign(payload, secret, { expiresIn: expiry })
    }

    static verify(token, secret = JWT_SECRET) {
        return jwt.verify(token, secret);
    }
}

_75f‍.d(JWTService);