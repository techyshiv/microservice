let Joi,RefreshToken,User,CustomErrorHandler,bcrypt,JWTService,REFRESH_SECRET;_bdb‍.x([["default",()=>_bdb‍.o]]);_bdb‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_bdb‍.w("../../models",[["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}],["User",["User"],function(v){User=v}]]);_bdb‍.w("../../services/customErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_bdb‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_bdb‍.w("../../services/JwtService",[["default",["JWTService"],function(v){JWTService=v}]]);_bdb‍.w("../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);






const loginController = {
    async login(req, res, next) {
        // [+] : Validate the Request
        _bdb‍.g.console.log("Data :", req.body);
        const loginSchema = Joi.object({
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}')).required(),
        });
        const { error } = loginSchema.validate(req.body);
        if (error) {
            return next(error);
        }
        // [+] : Check if a email exist in the database or not
        let user;
        try {
            user = await User.findOne({ email: req.body.email });
            if (!user) {
                return next(CustomErrorHandler.unAuthorized());
            }
        } catch (err) {
            return next(err);
        }

        // [+] : compare the password
        const match = await bcrypt.compare(req.body.password, user.password);
        if (!match) {
            return next(CustomErrorHandler.unAuthorized());
        }

        // [+] : Genearte Token
        let accessToken;
        let refreshToken;
        try {
            accessToken = JWTService.sign({ _id: user._id, role: user.role });
            refreshToken = JWTService.sign({ _id: user._id, role: user.role }, '1y', REFRESH_SECRET);
            // [+] : Save Refresh Token into an Database.
            await RefreshToken.create({ token: refreshToken });
        } catch (err) {
            return next(err);
        }

        // [+] send Token
        res.json({ accessToken: accessToken, refreshToken: refreshToken });

    }
}

_bdb‍.d(loginController);