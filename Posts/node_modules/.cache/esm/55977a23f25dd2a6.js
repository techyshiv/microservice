let ObjectID,Post;_f25‍.x([["default",()=>_f25‍.o]]);_f25‍.w("bson",[["ObjectID",["ObjectID"],function(v){ObjectID=v}]]);_f25‍.w("../models",[["Post",["Post"],function(v){Post=v}]]);

const postController = {
    async create_post(req, res) {
        let postDetails = req.body;
        const { title, description, author, user_id } = postDetails
        _f25‍.g.console.log(postDetails);
        const post = new Post({
            title,
            description,
            author,
            user_id
        })
        try {
            let result = await post.save();
            _f25‍.g.console.log("result :", result.user_id.toString());
            res.send({ "mess": "post created successfully !!" })
        } catch (error) {
            console.log("error while fetching data");
            res.json({
                "erro": "Error while post data"
            })
        }
    },

    async get_post(req, res) {
        _f25‍.g.console.log("request :", req.params.id);
        let isExist = await Post.find({ 'user_id': ObjectID(req.params.id) }).select('-_id -user_id -createdAt -updatedAt -__v');;
        if (isExist) {
            res.json({ "mess": "list of posts", "data": isExist })

        }
        else {
            res.json({ "mess": "list of posts", "data": isExist })
        }
    }
}

_f25‍.d(postController);