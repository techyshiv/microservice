let express,mongoose,routes,APP_PORT,DB_URL;_094‍.w("express",[["default",["express"],function(v){express=v}]]);_094‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_094‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);_094‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}],["DB_URL",["DB_URL"],function(v){DB_URL=v}]]);



const app = express();

// Database Connection
mongoose.connect(DB_URL, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
})

const DB = mongoose.connection;
DB.on('error', _094‍.g.console.error.bind(_094‍.g.console, 'connection error'));
DB.once('open', () => {
    console.log("Database Connected Successfully !!");
})

// Register all routes
app.use(express.json());
app.use('/api', routes);
app.listen(APP_PORT, () => {
    console.log(`Listening on Port :${APP_PORT}`);
})